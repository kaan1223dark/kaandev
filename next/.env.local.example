# Example .env.local for local development
# Copy this file to .env.local in the `next/` folder and fill in real values.
# DO NOT commit your real secrets to version control.

# Azure AD B2C / Azure AD provider
AZURE_P_CLIENT_ID=your-azure-client-id
AZURE_P_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=your-azure-tenant-id

# NextAuth secret (use a random long string)
NEXTAUTH_SECRET=some-random-secret-value

# Strapi CMS
NEXT_PUBLIC_STRAPI_BASE_URL=https://cms.example.com
STRAPI_API_KEY=your-strapi-api-key
# Optional preview token if you use Strapi preview endpoints
# STRAPI_PREVIEW_TOKEN=your-preview-token

# Application-specific IDs (replace with real UUIDs from your Strapi / config)
NEXT_PUBLIC_NO_ROLE_ID=00000000-0000-0000-0000-000000000000
NEXT_PUBLIC_LUUPPI_HATO_ID=00000000-0000-0000-0000-000000000000
NEXT_PUBLIC_LUUPPI_MEMBER_ID=00000000-0000-0000-0000-000000000000

# Misc
TOKEN_VERSION=1
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Instructions:
# 1) Copy this file to .env.local (in the same `next/` folder):
#      cp .env.local.example .env.local
# 2) Replace placeholder values with real ones (Azure app registration values, Strapi API key, IDs, etc.)
# 3) Restart the Next.js dev server so environment variables are reloaded:
#      npm run dev
# 4) To temporarily set in PowerShell for a single run (example):
#      $env:AZURE_P_CLIENT_ID = "your-azure-client-id"; $env:AZURE_P_CLIENT_SECRET = "..."; $env:AZURE_TENANT_ID = "..."; npm run dev

# Security note: Keep .env.local out of git. The repository should have .gitignore configured to ignore .env.local.
